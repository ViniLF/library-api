{
  "name": "library-api",
  "version": "1.0.0",
  "description": "API RESTful para sistema de gerenciamento de biblioteca digital",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "set NODE_ENV=test && jest",
    "test:watch": "set NODE_ENV=test && jest --watch",
    "test:coverage": "set NODE_ENV=test && jest --coverage",
    "test:unit": "set NODE_ENV=test && jest tests/unit",
    "test:integration": "set NODE_ENV=test && jest tests/integration",
    "test:ci": "set NODE_ENV=test && jest --ci --coverage --watchAll=false",
    "db:migrate": "npx prisma migrate dev",
    "db:generate": "npx prisma generate",
    "db:seed": "node prisma/seed.js",
    "db:studio": "npx prisma studio"
  },
  "keywords": [
    "api",
    "rest",
    "library",
    "node",
    "express",
    "prisma"
  ],
  "author": "Seu Nome",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "joi": "^17.9.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "prisma": "^5.2.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.3"
  }
}
